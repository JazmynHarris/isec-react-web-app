/**
 * This is where a user can create a new drawing through the website.
 * To save the drawing, they must sign in/up
 */
export default function Create() {
    // Should be able to create from the website
    //
    return (
        <div>
            <h1>Create A Drawing</h1>
            <button className="btn btn-lg">Create</button>
        </div>
    );
}